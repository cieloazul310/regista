import { defineGlobalStyles } from "@pandacss/dev";

export default defineGlobalStyles({
  pre: {
    p: "lg",
    borderRadius: "l2",
    overflowX: "auto",
  },
  "article pre": {
    my: "lg",
  },
});
